apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart
  namespace: microservices-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart
  template:
    metadata:
      labels:
        app: cart
    spec:
      containers:
      - name: cart
        image: saxenaaayush9000/cart-service:latest
        ports:
        - containerPort: 8085
        env:
        - name: SPRING_DATASOURCE_URL
          value: jdbc:postgresql://postgres:5432/MAPPINGDB
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: POSTGRES_USER
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: POSTGRES_PASSWORD
        - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
          value: http://eureka:8761/eureka
        - name: SPRING_CONFIG_IMPORT
          value: optional:configserver:http://config:8888
---
apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: microservices-app
spec:
  selector:
    app: cart
  ports:
    - port: 80
      targetPort: 8085